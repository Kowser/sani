<div class="panel panel-info">
  <div class="panel-heading">
    <h4 class="panel-title">Receive Payments</h4>
  </div>
  <div class="panel-body">
  	<%#= semantic_form_for @payment do |f| %>
  	  <div class='row'>
  	    <h4 class='text-center'>Resident's Information</h4>
  	    <div class='col-md-4 col-xs-12'>
  	      <%#= f.input :first_name %>
  	    </div>
  	    <div class='col-md-4 col-xs-12'>
  	      <%#= f.input :last_name %>
  	    </div>
  	    <div class='col-md-2 col-xs-6'>
  	      <%#= f.input :move_in, as: :string, label: 'Move In Date', input_html: { class: 'datepicker' } %>
  	    </div>
  	    <div class='col-md-2 col-xs-6'>
  	      <%#= f.input :rent, placeholder: '1234.56' %>
  	    </div>
  	    <div class='col-md-2 col-xs-6'>
  	      <%#= f.input :unit, placeholder: '6' %>
  	    </div>
  	  </div>
  	  <%#= f.submit 'Save Resident', class: 'btn btn-success' %>
  	<%# end %>
      remote form w/ javascript response that updates the table row respectively (what row id should we use)<br>
      disable button while JS is running in the background
  </div>
</div>

<div class="panel panel-success">
	<div class="panel-heading">
	  <span class="panel-title">All Comfort Residential Care - Resident Billings</span>
	</div>
	<table class="table table-condensed table-striped table-hover table-responsive">
		<thead>
			<tr>
				<th>Room</th>
				<th>Name</th>
				<th>Rent</th>
				<th>Amount Due</th>
				<th>Status</th>
				<th>Received</th>
				<th>Deposited</th>
			</tr>
		</thead>
	  <tbody>
	  	<% @residents.each do |resident| %>
	  		<tr>
		  	  <td><%= resident.unit.number %></td>
		   		<td><%= resident.name %></td>
		   		<td><%= resident.rent %></td>
		   		<td><%#= invoice.amount_due %></td>
		   		<td><%#= invoice.status %></td>
		   		<td></td>
		   		<td></td>
		  	</tr>
	  	<% end %>
	  </tbody>
	</table>
</div>

